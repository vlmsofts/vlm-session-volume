# ── Live data outputs & permanent history (data, not code) ──
data/

# ── Bloomberg seed: committed so the VLM gateway can serve it. Reseed with
# ── cotton_futures_volume_history_blpapi.py --merge to keep diffs small. ──

# ── Python bytecode / caches ──
__pycache__/
*.py[cod]
.pytest_cache/

# ── Runtime logs ──
logs/

# ── Editor / OS scratch ──
*.bak
*.tmp
*.swp
*~

# ── Secrets -- never commit ──
.env
.env.*
